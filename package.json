{
  "name": "recaller",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "RECALLER — AI agentic credit underwriter for thin-file green borrowers.",
  "scripts": {
    "app": "npm --prefix app run dev",
    "app:build": "npm --prefix app run build",
    "app:start": "npm --prefix app run start",
    "site": "npm --prefix frontend run dev",
    "site:build": "npm --prefix frontend run build",
    "test": "node --import ./scripts/alias.mjs --test scripts/test-engine.mjs",
    "test:agent": "node --import ./scripts/alias.mjs --test scripts/test-agent.mjs",
    "verify": "node --import ./scripts/alias.mjs scripts/verify-pipeline.mjs",
    "verify:verbose": "node --import ./scripts/alias.mjs scripts/verify-pipeline.mjs --verbose",
    "data:validate": "node --import ./scripts/alias.mjs scripts/validate-data.mjs",
    "check": "npm run data:validate && npm run test && npm run test:agent && npm run verify",
    "install:all": "npm --prefix app install && npm --prefix frontend install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
